#include <stdio.h>
/**
 * main - prints numbers from 0 to 9
 * Return: always 0 (Success)
 */
int main(void)
{
	int i;

	num []= "123456789"
	for (i = 0 ; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
