#include <stdio.h>
/**
 * main - prints numbers from 1 to 9
 * Return: always 0 (Success)
 */
int main(void)
{
	int i;

	char num[9] = "123456789"
	for (i = 0; i < 9, i++)
	{
		putchar([i]);
	}
	putchar('\n')
	return (0);
}
